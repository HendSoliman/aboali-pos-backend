spring.application.name=aboali-pos-backend
# ?? Server ??????????????????????????????????????
server.port=8080

# ?? PostgreSQL ??????????????????????????????????
spring.datasource.url=jdbc:postgresql://localhost:5432/aboali_pos
spring.datasource.username=pos_user
spring.datasource.password=pos_pass_1234
spring.datasource.driver-class-name=org.postgresql.Driver

# ?? JPA ?????????????????????????????????????????
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ?? Flyway ??????????????????????????????????????
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true

# ?? Jackson (Arabic UTF-8 safe) ?????????????????
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.default-property-inclusion=non_null

# ?? Swagger UI ??????????????????????????????????
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/api-docs
